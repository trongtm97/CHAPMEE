/** Shared spacing — keeps ads visually separate from content and navigation. */
export const AD_SLOT_SURFACE_CLASS =
  "my-6 w-full max-w-full clear-both overflow-hidden rounded-xl border border-white/5 bg-transparent";

export const AD_SLOT_IN_FEED_CLASS = `${AD_SLOT_SURFACE_CLASS} md:hidden`;

/** Narrow vertical rail beside reader layout (160px skyscraper). */
export const AD_SLOT_RAIL_CLASS = `${AD_SLOT_SURFACE_CLASS} mx-auto w-full max-w-[10rem] min-h-[36rem] xl:min-h-[37.5rem]`;
