/** db table for published Reels content. */
export const REELS_ITEMS_TABLE = "reels_items";

/** Form field for editing an existing Reels item. */
export const REELS_ITEM_ID_FIELD = "reel_id";

/** Feed entry discriminator for a Reels item (not a native ad). */
export const REELS_FEED_ENTRY_KIND = "reel" as const;
